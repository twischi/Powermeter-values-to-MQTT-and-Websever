#!/bin/sh
pip3 install zeroconf
# zeroconf: Python package provides an implementation of the Zeroconf protocol,
#           also known as Multicast DNS (mDNS) and DNS Service Discovery (DNS-SD).
#           The protocol allows devices on the same local network to discover each other 
#           and communicate without requiring a central server or manual configuration.